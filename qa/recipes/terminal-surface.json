{
  "name": "terminal-surface",
  "preset": "ui-terminal",
  "steps": [
    { "expect": { "state_path": { "path": "screen", "equals": "terminal" } } },
    { "expect": { "state_path": { "path": "section", "equals": "operator" } } },
    { "expect": { "state_path": { "path": "has_workspace", "equals": true } } },
    { "expect": { "node": { "role": "region", "name": "terminal", "exists": true } } }
  ]
}
