{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main-window IPC baseline: node_* commands plus the core event API the ducktape://block stream rides on.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
