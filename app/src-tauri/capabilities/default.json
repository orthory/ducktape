{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "IPC baseline for the console (main) and the macOS menu-bar popover (tray): node_* commands plus the core event API the ducktape://block stream and ducktape://navigate ride on.",
  "windows": ["main", "tray"],
  "permissions": ["core:default"]
}
