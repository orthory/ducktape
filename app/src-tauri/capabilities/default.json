{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Shared core/window IPC baseline for the console (main), macOS menu-bar popover (tray), and huddle window. High-authority workspace, identity, signing, and enrollment commands additionally enforce the main window label in Rust.",
  "windows": [
    "main",
    "tray",
    "huddle"
  ],
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging",
    "core:window:allow-close",
    "trusted",
    "agent:default",
    "notification:default"
  ]
}
