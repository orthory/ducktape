import React from "react";
import ReactDOM from "react-dom/client";

// placeholder root — the console UI lands in the next increment
ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <div>ducktape</div>
  </React.StrictMode>,
);
