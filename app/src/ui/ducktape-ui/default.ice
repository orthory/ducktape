// Vendored from ducktape-ui-lang crates/ui/src/ice/default.ice @ 3b34334, with
// upstream's `theme contract AppTheme` + `palette app` DELETED: 2.0 permits one
// contract and one palette per graph, and the app's is in `../theme.ice`, which
// carries these 46 kit roles plus the 74 the design artifact names.
use "recipes.ice"
use "components.ice"
