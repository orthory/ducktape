[package]
name = "forge-interface"
edition.workspace = true
version.workspace = true

# types-only: the wire surface of the forge (git-backed) module.
[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
