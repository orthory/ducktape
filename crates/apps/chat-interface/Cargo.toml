[package]
name = "chat-interface"
edition.workspace = true
version.workspace = true

# Slack-like chat wire surface backed by the messaging storage model.
[dependencies]
messaging-interface = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
