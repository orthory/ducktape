[package]
name = "vaults-interface"
edition.workspace = true
version.workspace = true

# the vaults module's public wire surface — types only. clients and sibling
# modules construct vault ops and queries from these shapes without linking the
# implementation.
[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
