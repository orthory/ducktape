[package]
name = "document-interface"
edition.workspace = true
version.workspace = true

# types-only: the wire surface of the document module. NO logic, NO sdk dep.
[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
