[package]
name = "agent-interface"
edition.workspace = true
version.workspace = true

# types-only: the wire surface of the agent module. reply blocks are the
# module's OWN vocabulary now, so the only cross-interface dependency left is
# saga-interface (the canonical origin mirror).
[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
saga-interface = { workspace = true }
