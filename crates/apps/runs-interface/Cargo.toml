[package]
name = "runs-interface"
edition.workspace = true
version.workspace = true

# types-only: the wire surface of the runs module. saga-interface supplies the
# canonical origin mirror carried by pending-run views.
[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
saga-interface = { workspace = true }
