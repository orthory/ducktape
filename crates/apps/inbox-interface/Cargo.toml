[package]
name = "inbox-interface"
edition.workspace = true
version.workspace = true

# types-only: the wire surface of the inbox module.
[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
