[package]
name = "capability"
edition.workspace = true
version.workspace = true

# the network-wide registry of node host capabilities as replicated state.
# depends only on sdk + its own interface (+ valset wire types to gate
# announcements to current members, sha2 for the state-based root).
[dependencies]
sdk = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
valset = { workspace = true }
sha2 = { workspace = true }
async-trait = { workspace = true }

[dev-dependencies]
futures = "0.3"
