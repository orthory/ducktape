[package]
name = "saga-interface"
edition.workspace = true
version.workspace = true

# types-only: the wire surface of the saga module + the worker-request effect.
[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
