[package]
name = "governance-interface"
edition.workspace = true
version.workspace = true

# the governance module's public wire surface — types only. the legal
# cross-module boundary: other modules and clients construct proposals, votes,
# and queries from these shapes without linking the governance implementation.
[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
