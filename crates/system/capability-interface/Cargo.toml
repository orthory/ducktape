[package]
name = "capability-interface"
edition.workspace = true
version.workspace = true

# types-only: the wire surface of the capability module. capability tags are
# open-set strings (validated by the impl crate, not here); node identity is
# the verified submit origin, never payload data.
[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
