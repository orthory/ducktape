# Desktop font identity and the product type scale. Shared color, shape, and
# component tokens live in `ducktape-ui`.
[package]
name = "design"
edition.workspace = true
version.workspace = true
publish = false
