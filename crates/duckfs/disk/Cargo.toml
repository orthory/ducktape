[package]
name = "duckfs-disk"
edition.workspace = true
version.workspace = true

# Native disk persistence for duckfs. Depends on duckfs-core, but not sdk.

[dependencies]
duckfs-core = { workspace = true }
sha2 = { workspace = true }

[dev-dependencies]
tempfile = { workspace = true }
