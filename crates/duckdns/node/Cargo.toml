[package]
name = "duckdns-node"
edition.workspace = true
version.workspace = true

# Node-side DuckDNS discovery announcement pump. Endpoints and transports stay
# in the reachability and data-plane crates.
[dependencies]
duckdns = { workspace = true }
host = { workspace = true }
sdk = { workspace = true }
node = { workspace = true }
