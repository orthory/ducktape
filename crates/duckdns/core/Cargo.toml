[package]
name = "duckdns-core"
edition.workspace = true
version.workspace = true

# Deterministic DuckDNS core: wire types, hostname grammar, replicated registry,
# canonical state codec, and in-memory state machine. No sdk and no host I/O.
[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
sha2 = { workspace = true }
