[package]
name = "greeter"
edition.workspace = true
version.workspace = true

# a CONSUMER module: composes directory + kv purely through their crate-root
# wire types — it never reaches their module internals.
[dependencies]
sdk = { workspace = true }
directory = { workspace = true }
kv = { workspace = true }
async-trait = { workspace = true }
