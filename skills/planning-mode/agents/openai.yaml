interface:
  display_name: "Planning Mode"
  short_description: "Plan judgment-heavy repo work before editing."
  default_prompt: "Use $planning-mode before writing or changing code in Ducktape, especially for multi-step work or delegation decisions."
