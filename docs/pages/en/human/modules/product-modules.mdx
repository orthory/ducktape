# Product Modules

Product modules are the workplace surface hosted by the platform. They share the
same module contract, but each module chooses the storage substrate and interface
that fits its job.

## Built or Started

- `pages` stores nested block-based pages, page tabs, and comment threads on
  QMDB, and exposes QMDB state sync.
- `forge` stores git-backed project state, with a root derived from the git head
  and pack install guarded by connectivity verification.
- `chat` is the storage-owning chat module: channels, threads, edits, reactions,
  and membership as one QMDB record per entity, with hook fan-out to subscribed
  modules.
- `agent` is a pure state machine holding the agent registry, channel watches,
  and run records.
- `tasks` stores a small canonical task list with staged writes.
- `vaults` replicates opaque secret ciphertext with owner-gated writes.
- `inbox` stores per-member notification queues delivered as follow-up ops.
- `automations` stores operator-defined rules that run over the chat hook seam.
- `files` is duckfs: a consensus-replicated, copy-on-write, content-addressed
  filesystem — every node holds every byte, and bytes survive restart.
- `jobs` is a consensus-native work board with first-claim-wins claims.
- `identity` binds node keys to user keys — a replicated registry so a person
  running several nodes still resolves to one identity above mesh/valset.
- `kv`, `valset`, `saga`, `governance`, `directory`, and `greeter` support
  infrastructure, examples, and tests.

## Chat And Huddles

Chat owns channels, message threads, edits, reactions, membership, and hook
fan-out. It stores one QMDB record per entity instead of one whole-history
value, which keeps hot channels from becoming commit bombs and lets reads page
by computed keys.

Huddles are attached to chat channels. Consensus stores the roster with
`join_huddle`, `leave_huddle`, and `sweep_huddle`; media state such as mute,
camera, and peer beacons stays ephemeral. The live node has a typed call socket
and voice/video hub: audio and control ride the voice lane, camera fragments ride
their own video lane, and the app renders a docked or popped-out huddle card.
Current admission is intentionally local-live-flow based; harder roster-derived
media admission remains a security hardening seam.

## Pages

Pages is the block editor module. It stores page roots, nested page parents,
blocks, and comment threads in the same QMDB-backed module, so page content and
comments state-sync together. Writes go through `CreatePage`, `SetPageParent`,
`DeletePage`, `InsertBlock`, `UpdateText`, `SetKind`, `SetChecked`,
`MoveBlock`, `RemoveBlock`, `AddComment`, `EditComment`, `DeleteComment`, and
`ResolveThread`; reads include page/block lookups, page lists, and comment-thread
queries for a page or visible block set.

The UI mirrors that shape: a nested page tree, page tabs backed by
`openTabs`, focus-light block editing, per-block comment affordances, and a
right-hand comments panel. Comments are plain text for now and live in `pages`
rather than a separate `comments` crate, which is the implemented divergence from
the earlier redesign plan.

## Agents And Runs

The agent stack is split deliberately. `agent` is the registry: display names,
executor requirement, prompt pins, permissions, status, watches, and run records.
`runs` composes prompts from chat context, submits dispatch recipes, records
responses, watches channels, and can opt into the jobs board. `dispatch` and
`saga` own capability-routed execution and strict leases, so model output returns
as ordered data rather than hidden local state.

The node discovers local capability providers from operator specs, announces the
truthful tag set to the `capability` module, and runs a dispatch worker only for
leases assigned to its own key. The console joins that state back together:
members show announced executor tags, agents use a registry-backed **Runs on**
picker, and in-flight runs can show the node currently holding the saga lease.

## Inbox

The inbox holds per-member notification queues as consensus state: a
seq-numbered item list plus a read cursor per member. Writes go through
`Deliver`, `MarkRead`, and `Clear`; reads through `List` and `Unread`. Caps are
enforced at execute with rejection (a 64-byte `kind` cap, a 16 KiB `body` cap,
bounded member count), with one deliberate exception: a delivery past the
per-member item cap drops the oldest item deterministically — it is a
notification queue, not a ledger.

The distinctive mechanism is atomic-with-cause delivery. Other modules deliver
notifications as follow-up ops, so a notification commits in the same block as
the event that caused it (platform promise P2). There is no external push
service — the queue is the delivery, which is also the air-gap-native
notification story. Each item's `source` records the delivering origin — a
module id, `ext:` plus lowercase hex for an external submitter, or `system` —
derived by the module and never caller-supplied.

## Automations

Automations stores operator-defined rules — a trigger over chat post filters
plus an action that posts a chat message or creates a task — alongside a bounded
run-history ring. Rule administration goes through `CreateRule`, `SetEnabled`,
and `DeleteRule`; reads through `ListRules`, `GetRule`, and `RunHistory`. Hook
events are origin-gated: only `Origin::Module("chat")` reaches the hook arm, so
a submitter cannot forge one.

A rule fires only when the post author is a user. Posts authored by modules or
agents — including this module's own follow-ups — never trigger rules, so an
automation posting into a hooked channel cannot cascade. Each event has an
action budget of eight, and every action is probed before it is emitted:
host-routed queries verify that a target channel exists and that the composed
message or task id is unused, closing the id-squatting move where a user
pre-posts the composed id to wedge the rule. A failed probe or a failed
message-text fetch downgrades to a run record with `action_ok = false` instead
of aborting the posting user's block; whatever the probes cannot see still
commits or aborts with the triggering post as one atomic unit (P2).

## Files

Files is duckfs: a consensus-replicated, copy-on-write, content-addressed
filesystem. Every node holds every byte as consensus state — bytes travel
through blocks and survive restart — so a committed file is readable on any
node, not a promise pinned to whichever node happened to hold the chunks. State
splits into an immutable object store (chunks, files, trees, and snapshots, all
content-addressed) and a small mutable refs cell (the live head, the pin table,
a bounded history window, the staging table, and the watch set); the root
hashes the refs cell alone and commits to the whole tree transitively. Two
writable roots gate authority: `/home/<owner>/**` is writable only by the
matching origin-derived owner, `/shared/**` by any member; reads are
cluster-visible everywhere — `/home` gates writes only.

Writes stage, then commit. `PutBlob` stages one raw chunk (a binary op frame,
quota'd per owner, expiring deterministically if never referenced); `Commit` is
the atomic unit — a batch of path changes (`Put`, `Mkdir`, `Rm`, `Mv`,
`Symlink`) under per-path compare-and-swap against a base snapshot: every
changed path must be untouched between the base and the live head or the whole
commit rejects, so disjoint concurrent commits interleave without false
conflicts and overlapping ones fail loudly. A successful commit appends an
immutable snapshot — copy-on-write, so unchanged subtrees are shared by hash
across versions — advances the head, and fans out watch notifications to
subscribed modules in the same block (P2). `Pin` and `Unpin` keep named
snapshots alive past the bounded history window. Caps are enforced at execute
with rejection, so an oversized value never enters the root preimage.

Reads are snapshot-addressable pure functions: `Stat`, `Ls`, `Read`
(byte-range), `Find`, `Grep`, `History`, `Diff`, and `Refs`, each cursor-paged
and answerable at the head or at any resolvable snapshot. duckfs is also the
agent memory — it absorbed the deleted `memory` module, and what memory did
with immutable generations, duckfs does with snapshots. Agent workspaces are
ordinary files under `/home` and `/shared`; `Grep` hits carry citable
`duck://files/<path>@<snapshot>#L<line>` evidence URIs; per-file meta powers
conventions like `kind=skill` (shared skills live under `/shared/skills/`); and
prefix watches with segment-aware matching (`/a` matches `/a/b`, never `/ab`)
give any module change notifications delivered atomically with the triggering
commit.

## Jobs

Jobs is a consensus-native work board for on-platform actors: humans and agent
fleets submitting ordinary signed ops. A submitter posts a job, any worker
claims it, and exactly one claim wins by consensus order — the total order is
the lock, and a losing claim fails its block loudly on every node; that
rejection is the race-resolution signal. The lifecycle runs submit to pending,
claim to processing under a view lease, then finalized as done or failed. Writes
go through `Submit`, `Claim`, `Finalize`, `Release`, `Reclaim`, `Cancel`, and
`Prune`, `RegisterWorker`, and `UnregisterWorker`; reads through `Get`, `List`,
and `Counts`, answered from committed state only.

Once a claim's view lease expires, anyone may `Reclaim` the job — the reclaim is
permissionless — returning it to pending; attempts are capped at eight, and
`Cancel` and `Prune` are submitter-only. Jobs is distinct from saga: jobs is the
product work board for actors acting over signed RPC ops, while saga is the
platform's reactor-effect ledger for off-consensus work the node's own worker
loop performs. Jobs never touches saga or the reactor.

Workers are consensus state, not genesis config. A worker module self-registers
by emitting `RegisterWorker` to jobs as a module-origin follow-up; external and
system origins cannot name arbitrary workers. The worker set is capped at 16.
Every successful `Submit` emits one `JobsEvent::Submitted` follow-up to each
registered worker in the same block. That fan-out counts against the host's
block dispatch budget.

**Cascade rule:** this slice supports one claiming worker for a given job kind.
If two registered workers both claim the same submitted job inside the submit
cascade, the second `Claim` observes the first staged claim, rejects, and aborts
the entire submit block under P2. `MAX_WORKERS` exists for later off-cascade
strategies, not for multiple in-cascade claimants of the same kind.

## Identity

Identity is the user/node binding registry (`crates/system/identity`): a user
is an ed25519 keypair held by the person in the app; a node is a workspace's
mesh/valset identity. The module stores, per user, a display name, a
replay-guard nonce, and the set of bound node keys — one person can bind
several nodes to the same user.

`BindNode` binds the SUBMITTING node (the verified origin, never a payload
field) to a user, consented to by a user-key signature over a
chain-id ‖ node-key ‖ nonce preimage — a certificate can never replay onto a
different network, a cert minted for node A can never bind node B, and a
stale nonce (from a prior bind/unbind) rejects. On
the networked node, `BindNode` is additionally gated to current members: the
origin must be a validator or an observer, so a node with no standing at all
cannot mint itself a user. `UnbindNode` carries no such gate and is accepted
from ANY external origin — a surviving device can evict a lost one — and it
bumps the user's nonce so a captured bind certificate for the evicted node is
dead forever; unbinding a user's last node keeps the user record — name and
nonce persist with zero nodes, so a later bind resolves the same identity.
`SetUserName` is origin-gated only (a bound node is trusted
hardware, no user signature required): it sets the display name of the user
the submitting node is bound to, under the same 64-byte limit as profiles,
and it does not bump the nonce — no user signature is consumed.

Binding does not change what stays per node: mesh tracking, valset seats, and
quorum arithmetic are untouched by this module. A user running three
validators still holds three seats and casts three votes — identity maps keys
to a person for display and authorship resolution, it does not collapse
consensus weight.

Adding identity to a network that predates it is a module-set change like any
other: the genesis app-hash moves, so an already-running network needs a
coordinated rebuild — a lockstep upgrade across every validator, not a rolling
one — to adopt it.

The module stores keys; custody of the user key itself is a desktop-app
concern layered on top (`bin/node`'s `user-key` verbs; see
`docs/superpowers/specs/2026-07-07-identity-onboarding-design.md`). The
24-word mnemonic is an entropy-preserving, identity-preserving encoding of the
raw ed25519 seed, so it recovers the identity exactly rather than deriving a
new one. The password is local-encryption only — argon2id wraps a key that
encrypts the seed at rest with XChaCha20-Poly1305 — and never enters key
derivation, so a forgotten password just means restoring from the mnemonic
and setting a new one. Node keys deliberately get neither treatment: they
stay plaintext and disposable, because consensus signs with a node key on
every view and a boot must never block on a password prompt; unbind-and-rejoin
is the recovery story for a lost or compromised device, and OS full-disk
encryption is the documented answer for at-rest protection of the node key.

## Interface Shape

Interface crates encode and decode module-specific payloads while the host
routes only opaque bytes. That keeps module dependencies narrow: product modules
share typed messages and queries, not implementation APIs.

## Product Pressure

The currently useful pressure points are clear from the checked-in modules:
chat exercises live product reads over QMDB-backed channel storage, forge
exercises authenticated project state, and the module wave — inbox, automations,
files, jobs — exercises follow-up atomicity, hook fan-out, and fully replicated
consensus bytes with snapshot history that fuller collaboration features will
lean on.
