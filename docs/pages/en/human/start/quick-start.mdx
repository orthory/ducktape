# Quick Start

The repository is a Rust workspace. The docs are a separate Vocs project under
`docs/`, so Rust verification and docs verification stay decoupled.

## Platform Checks

Run the workspace tests:

```sh
cargo test --workspace
```

Run the in-process super-app demo:

```sh
cargo run -p demo
```

Run the invite/join/admit real-socket node demo:

```sh
cd bin/node
./examples/demo-invite.sh
```

Run the joiner state-sync proof directly:

```sh
cargo test -p demo --test joiner_rebuilds_global_app_hash
```

## Docs Checks

Install the docs dependencies and run the docs gate:

```sh
cd docs
bun install
bun run docs:check
```

Start Vocs locally:

```sh
cd docs
bun run dev
```

The docs project is intentionally rooted at `docs/` with `srcDir: "."`. Page
files live in `docs/pages`, not in a nested `docs/docs` directory.
