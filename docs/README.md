# Ducktape Docs

This directory is the Vocs project for the Ducktape public documentation.

The Vocs config intentionally sets `srcDir: "."`, so pages live at
`docs/pages`. Do not create `docs/docs`.

## Commands

```sh
pnpm install
pnpm run docs:check
pnpm run dev
```
