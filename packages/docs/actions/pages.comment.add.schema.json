{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "pages.comment.add",
  "description": "Add a comment anchored to a block or page: open a new thread (omit thread_id) or reply within an existing one. Thread and comment ids are minted deterministically by the docs-harness module.",
  "type": "object",
  "properties": {
    "target": {
      "type": "string",
      "description": "The block or page id the comment (or its thread) is anchored to."
    },
    "thread_id": {
      "type": "string",
      "description": "Existing thread to reply in; omit to open a new thread on the target."
    },
    "text": {
      "type": "string",
      "minLength": 1,
      "maxLength": 4096,
      "description": "The comment body."
    }
  },
  "required": ["target", "text"],
  "additionalProperties": false
}
