{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "pages.thread.resolve",
  "description": "Resolve or reopen a page comment thread.",
  "type": "object",
  "properties": {
    "page_id": {
      "type": "string",
      "description": "The page that owns the thread."
    },
    "thread_id": {
      "type": "string",
      "description": "The thread to resolve or reopen."
    },
    "resolved": {
      "type": "boolean",
      "description": "true to resolve the thread, false to reopen it."
    }
  },
  "required": ["page_id", "thread_id", "resolved"],
  "additionalProperties": false
}
