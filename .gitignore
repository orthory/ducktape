/target
# labs is a standalone workspace (excluded from the root), so its build output
# lands outside the root-anchored /target above.
/crates/labs/target
/.worktree/
**/*.rs.bk
.DS_Store
.claude/worktrees/
.codex/worktrees/
node_modules
docs/dist/
docs/.astro/
docs/.nimbus/
app/dist/
ops/wg-smoke/*.log
ops/wg-smoke/node*/

# superpowers SDD scratch
.superpowers/
package-lock.json
