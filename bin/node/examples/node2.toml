# node 2 — the THIRD validator. dials node 0; identical mesh + validator sets.
id = 2
listen = "127.0.0.1:52202"
namespace = "ducktape-demo"
# mesh = validators + the sync-only joiner (seed 3); consensus is [0, 1, 2].
peer_seeds = [0, 1, 2, 3]
validator_seeds = [0, 1, 2]
bootstrapper_addr = "127.0.0.1:52200"
storage_dir = "/tmp/ducktape-2"
rpc_listen = "127.0.0.1:52302"
