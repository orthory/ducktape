[package]
name = "beacon-collect"
version = "0.1.0"
edition = "2021"

[dependencies]
iced_beacon = "=0.14.0"
tokio = { version = "1", features = ["rt-multi-thread", "macros"] }
futures = "0.3"

[workspace]
